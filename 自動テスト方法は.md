自動テスト方法は

自動テストを実行する手順は、以下の 3ステップ です。

Step 1: Webサーバーを起動する
テスト対象のWebアプリ（http://localhost:8000）を起動します。

bash
# プロジェクトフォルダでPHPサーバーを起動
php -S localhost:8000
(すでにサーバーが起動している場合は不要です)

Step 2: 必要なパッケージのセットアップ（初回のみ）
ターミナルを開き、以下のコマンドで自動テストツール（Playwright）を準備します。

bash
# 依存ライブラリのインストール
npm install
# 自動テスト用ブラウザのインストール
npx playwright install
Step 3: 自動テストを実行する
① バックグラウンドで高速実行する場合（基本）
bash
npx playwright test
② 実際にブラウザが動く様子を見ながらテストする場合
bash
npx playwright test --headed
③ UIモード（GUI画面付きでステップごとに確認・デバッグする場合）
bash
npx playwright test --ui
テスト結果の確認
テストが終了すると、ターミナルに以下のような結果が出力されます。

text
✓  1. 初期表示の要素検証 (450ms)
  ✓  2. タスク追加フロー (820ms)
  ✓  3. 検索フィルター動作テスト (300ms)
  ✓  4. フィルタートグル動作 (280ms)
  4 passed (2.1s)
失敗した場合は自動的にスクリーンショットが生成され、原因となったエラー行を確認できます。

7:31 PM
